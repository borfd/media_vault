== README

A simple app to add links to a media vault. Supports scraping the title of the web page and client-side filter of posts. Youtube links and links to images are also shown appropriately.

=== Setup

```bash
rake db:setup

rake db:migrate

rails s
```
